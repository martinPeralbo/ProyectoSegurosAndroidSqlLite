<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pruebasqllite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MostrarVendedor"></activity>
        <activity android:name=".MostrarContrato" />
        <activity android:name=".RegistrarVendedor" />
        <activity android:name=".VendedoresList" />
        <activity android:name=".Contratoslist" />
        <activity android:name=".CrearContratoConCliente" />
        <activity android:name=".Prueba" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <activity android:name=".Userlist" />
    </application>

</manifest>